//Ember provides an adapter for browser local storage. Using this meant I didn't have to
//set up a backend (which wasn't the purpose of working through this app)

export { default } from 'ember-local-storage/adapters/local';