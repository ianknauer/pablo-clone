import Ember from 'ember';

export default Ember.Component.extend({
  // This is where you define the fonts that are available in the text-editor font dropdown. They're pulled in from google fonts.
  fonts: ['Roboto', 'Merriweather'],
});
